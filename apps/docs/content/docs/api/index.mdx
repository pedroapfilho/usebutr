---
title: Overview
description: Every public export, organised by package. Signatures verified against each package entry point.
---

This reference lists every public export, per package. It's hand-written and
mirrors each package's `src/index.ts` — nothing here is generated or
speculative.

| Package                  | Reference                                                        |
| ------------------------ | ---------------------------------------------------------------- |
| `@usebutr/core`          | [Types, store, storage, discovery seam](/api/core)               |
| `@usebutr/react`         | [Provider and every hook](/api/react)                            |
| `@usebutr/evm`           | [EIP-6963 discovery, adapter builder, chains, helpers](/api/evm) |
| `@usebutr/svm`           | [Wallet Standard discovery, adapter builder, chains](/api/svm)   |
| `@usebutr/wallets`       | [Auto provider, composed discovery, chains](/api/wallets)        |
| `@usebutr/walletconnect` | [`createWalletConnectAdapter`](/api/walletconnect)               |
| `@usebutr/ledger`        | [`createLedgerAdapter`](/api/ledger)                             |

`@usebutr/testing` is documented in the [Testing guide](/testing).
