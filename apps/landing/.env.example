# Base URL of the docs site, used by the landing page links.
# Optional; when unset it falls back to https://docs.usebutr.com.
# NEXT_PUBLIC_DOCS_URL=

# Base URL of the demo app, used by the landing page links.
# Optional; when unset it falls back to https://demo.usebutr.com.
# NEXT_PUBLIC_DEMO_URL=
